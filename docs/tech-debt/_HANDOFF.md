# Handoff — findings that need a file their agent did not own

Append only. One line per item, in the format:

```
- [ ] <finding-id> — needs `<file you don't own>` — <one line on what remains>
```

A `- [x]` means the item is closed; the note appended to it says by whom and how,
including where a pass found the original description understated the work or got
it wrong. An unticked line with a **Progress**/**Note**/**Update** clause is still
open, but its prerequisites have moved.

**The final sequential pass (2026-08-18)** — full write access to `src/`,
`examples/` and `Cargo.toml`, and the last pass on this audit. It closed
`serde-005`, `async-003`, `async-004`, `obs-001`, `obs-002`, `obs-004`, `obs-011`,
`obs-014`, `mem-001`, `mem-008`, `lint-009`, `test-005`, `test-007`, `type-004`'s
`CatalogId` half, `type-005`, `type-010`, `proj-003`'s evidence half, `num-001`,
`serde-002`, `serde-003`, and the `trait-` pointer on `CaptureStop::stop`. It
**declined** `mem-002`'s `SmallVec` half, `mem-009`'s `ArrayVec` half, `obs-012`,
and `type-010`'s closed-kind-enum shape — each with a measurement or a design
argument at its line, and in the last case with a *different* fix applied in its
place. It left every structural split (`proj-004`/`005`/`009`, `test-006`,
`test-011`), `type-003`'s domain half, `type-008`, `lint-008` and `api-005` alone,
as instructed. Gates after it: **589 passed / 0 failed / 2 ignored** on
`cargo test --locked --all-features`, clippy **0 diagnostics** on all six CI
feature lanes, `cargo fmt --check` clean, `cargo doc --locked --no-deps
--document-private-items` at **0 warnings**, and all nine `--locked` lanes green
after the `Cargo.lock` refresh `test-005` required.

**The API-shape/naming/visibility pass (2026-08-18)** closed `api-001`, `api-002`,
`api-007`, `api-008`, `name-001`, `name-002`, `name-003`, `err-004`, `err-008`,
`conv-004`, `const-004`, `own-003`, `doc-009`, `lint-001`, `lint-003`, `lint-004`,
`lint-006`, `lint-007` and half of `lint-009`; it **declined** `lint-008` and
`api-005` with reasons at those lines, and left `proj-004`/`005`/`009` alone as
file splits excluded from that run. Gates after it: **577 passed / 0 failed / 2
ignored**, clippy silent on all six CI feature lanes, `cargo fmt --check` clean,
`cargo doc --locked --no-deps --document-private-items` at zero warnings.

## From the manifest/CI/docs agent (`Cargo.toml`, `rust-toolchain.toml`, `deny.toml`, `justfile`, `build.rs`, `.github/**`, `README.md`, `docs/capture-backend-choice.md`, `docs/initial-stream-anchor.md`, `config.example.toml`)

- [x] `lint-001` — needs `src/migrate.rs`, `src/actuator/mod.rs`, `src/actuator/win.rs` — **the rustdoc gate is landed and is RED until these three edits are made.** `[lints.rustdoc]` in `Cargo.toml` now denies `broken_intra_doc_links` and `redundant_explicit_links`, a `doc` recipe is in `justfile`'s `verify`, and a `Check the documentation cross-references` step is in `.github/workflows/ci.yml`. Required: `src/migrate.rs:81` ``[`crate::main`]`` → plain ``` `main` ``` (no intra-doc link into a binary target exists); `src/actuator/mod.rs:58` ``[`setup`]`` → ``[`crate::app::setup`]``; `src/actuator/win.rs:561` drop the redundant explicit target so it reads ``the [`shield`] over the game until [`release`](Surface::release)`` (doc-008 wants the second half of that same line changed too). Verify with `cargo doc --locked --no-deps --document-private-items`; it must print zero warnings. **DONE (verified by the api-shape pass).** All three edits are in the tree and `cargo doc --locked --no-deps --document-private-items` prints zero warnings; the rustdoc gate is green.
- [x] `lint-003` — needs `src/lib.rs`, `src/main.rs` — add the crate-level ratchet that cannot go in `Cargo.toml`'s `[lints]` table (the table also applies to the test harness, where 257 `unwrap`s and 19 `panic`s live). `clippy::todo`/`unimplemented`/`dbg_macro`/`mem_forget`/`exit` are already in the table because they measure zero in tests too, so only these two remain. Verbatim, in **both** files: **DONE (verified by the api-shape pass).** The `#![cfg_attr(not(test), warn(clippy::unwrap_used, clippy::panic))]` ratchet is at `src/lib.rs:26` and `src/main.rs:19`, verbatim with its comment.
  ```rust
  // Shipped code has none of these today (measured: 0 sites in --lib --bins).
  // `not(test)` keeps the ratchet off the test harness, where `unwrap` in a
  // fixture is the correct spelling — 257 sites and rising. The rest of the
  // lint policy lives in Cargo.toml's `[lints]`; these two cannot, because a
  // `[lints]` table applies to every target including the tests.
  #![cfg_attr(not(test), warn(clippy::unwrap_used, clippy::panic))]
  ```
- [x] `lint-006` — needs `src/actuator/win.rs:502`, `src/capture/mod.rs:80,82`, `src/migrate.rs:261`, `src/stream.rs:421,423` — drop the redundant `std::mem::` prefix (in the Rust 2024 prelude since 1.80). Once all six are gone, add `unused_qualifications = "warn"` to `[lints.rust]` in `Cargo.toml`; it was left out only because 6 live sites × `-D warnings` = red CI. **DONE (api-shape pass).** `unused_qualifications` measures **0** across `--all-targets --all-features` and on all six CI feature lanes (the 21 test-harness sites this item's later measurement found are fixed too, via `cargo clippy --fix`), and `unused_qualifications = "warn"` is now in `[lints.rust]`.
- [x] `lint-007` / `proj-010` — needs `src/ui/view.rs:9,40,53`, `src/ui/editor/mod.rs:29,52` — make the five `pub` items `pub(super)`. Then add `unreachable_pub = "warn"` to `[lints.rust]`; held back for the same reason (5 live sites). **DONE (already landed + enforced).** All five items were already `pub(super)` when triaged; `unreachable_pub` measured 0, and `unreachable_pub = "warn"` is now in `[lints.rust]`.
- [ ] `lint-008` — needs `src/render.rs` (or `src/lib.rs:24`) — pick one direction for the 13 `pub(crate)` items in the private `mod render`, then add `redundant_pub_crate = "warn"` to `[lints.clippy]` (14 live sites today). **DECLINED (api-shape pass), with the coupling resolved rather than deferred.** `redundant_pub_crate`'s only suggestion for these items is `pub`, and applying it to `render.rs`'s 13 makes **all 13 fire under `unreachable_pub` instead** (measured by actually applying the rewrite). For an item inside a private module the two lints have no common fixed point, and `pub` is also the *less* informative spelling — it hides that these are crate-internal, and in `capture/pcap.rs` it widens the apparent audience of the `unsafe impl Send` argument, which is the cost that report already flagged. `unreachable_pub` (the one that catches a real error rather than a spelling) is enabled instead and measures 0; the reasoning is recorded beside it in `Cargo.toml`. The 15 `pub(crate)`s stay as written. Note for a future pass: `domain::control::watchdog`'s new `past_rung` would be a 16th, and it genuinely needs `pub(crate)` for the re-export to work — i.e. a false positive.
- [x] `lint-009` — needs `src/migrate.rs`, `src/capture/pcap.rs`, `src/actuator/win.rs` (+5 more) for `doc_markdown` (18 sites), `src/render.rs:130-160` for `format_push_string` (6), `src/main.rs:307` for `redundant_clone` (1) — the only three pedantic/nursery lints worth enabling. Add each to `[lints.clippy]` in `Cargo.toml` **after** its sites are fixed. Do not enable the other eight; `25-lint.md`'s second table argues each one down with counts. **PARTIALLY DONE (api-shape pass).** `redundant_clone = "warn"` and `format_push_string = "warn"` are in `[lints.clippy]`; both measure 0 across all six CI lanes (`format_push_string`'s six `render.rs` sites were already gone). `doc_markdown` is **not** enabled and is one line from being able to be: `src/config/persist.rs:390` (`dry_run`/`backend`), `src/domain/control/mod.rs:292` (`DryRun`) and `src/actuator/win.rs:1808` (`WinDbg`) are backticked, and `src/domain/filter.rs:19`'s `ANDed` — a false positive, since backticking English prose would be wrong — is reworded to "combined with a logical AND". The single remaining site is **`build.rs:35`** (`WinDivert` → backticks), which is outside the file grant this pass was given. Fix that one word and `doc_markdown = "warn"` can land at 0. **DONE (final pass).** `doc_markdown = "warn"` is in `[lints.clippy]` and measures **0** on all six CI lanes. The `build.rs:35` site this line was waiting on was already backticked when triaged, so the only remaining work was enabling the lint and verifying it — including that `cargo clippy` does not lint `build.rs` itself, so the entry cannot redden a lane from there either. All three of this item's lints are now enabled; the other eight stay argued down in `25-lint.md`.
- [x] `lint-004` — needs `src/capture/pcap.rs:239,768,789`, `src/migrate.rs:183,258` — after the `unsafe-` P2 work splits those five blocks, add `multiple_unsafe_ops_per_block = "warn"` to `[lints.clippy]`. `undocumented_unsafe_blocks = "warn"` is already in, at 0 sites. **DONE (api-shape pass).** One site had reappeared since the capture agent measured — `src/actuator/win.rs:278`, a two-call `GetAwarenessFromDpiAwarenessContext(GetThreadDpiAwarenessContext())` block from the DPI-verification commit — now split one call per block with its own `// SAFETY:`. `multiple_unsafe_ops_per_block = "warn"` is in `[lints.clippy]`.
- [ ] `proj-009` — needs `src/config.rs` → `src/config/mod.rs` (and its `include_str!("../config.example.toml")` → `../../`) — after the move, add `mod_module_files = "warn"` to `[lints.clippy]` so the nine-to-one `mod.rs` convention is enforced rather than merely prevalent. **OUT OF SCOPE for the api-shape pass** (module move). Measured while triaging: `clippy::mod_module_files` is at **9** sites today — `src/{actuator,app,capture,domain,ui,uplink}/mod.rs`, `src/app/session/mod.rs`, `src/domain/control/mod.rs`, `src/ui/editor/mod.rs` — so the nine-to-one convention is real and `config.rs` is the one exception, exactly as this item says. Still open.
- [x] `test-005` / `test-004` — needs `Cargo.lock`, and a decision that belongs to `src/crash.rs`'s owner — `Cargo.toml:95`'s `egui_kittest = { …, features = ["wgpu", "snapshot"] }` carries a `snapshot` feature nothing uses (no `assert_snapshot`/`try_snapshot` call, no `snapshots/`, no `.snap`), contradicting the manifest's own stated reason for restricting `eframe` to `glow`. The edit is `features = ["wgpu"]`, but it is a **dependency-graph** change: it drops `dify`/`open`/`tempfile` from the resolution, so `Cargo.lock` must be regenerated or all nine `--locked` lanes fail — and `Cargo.lock` is nobody's file in this wave. It is also coupled to `test-004`: if that finding is fixed by option (b) (`tempfile = "3"` as a direct dev-dep, which is what keeps `tempfile` compiled once `snapshot` is gone) both manifest edits must land in the same commit as the lock refresh. Deliberately not done here rather than churning the lock mid-run. **DONE (final pass).** `egui_kittest` is `features = ["wgpu"]`, honouring `crash.rs`'s owner's decision: option (b) was **not** taken and `tempfile` is not a dependency. `Cargo.lock` regenerated in the same change — **14** crates left the resolution (`dify`, `open`, `tempfile`, `rayon`, `rayon-core`, `crossbeam-deque`, `crossbeam-epoch`, `anyhow`, `colored`, `fastrand`, `getopts`, `is-docker`, `is-wsl`, `pathdiff`), and all nine `--locked` lanes were re-run green afterwards.
- [x] `obs-004` — needs `src/main.rs:335-338` first — once `eframe::run_native`'s failure emits `tracing::error!(… "the application window could not be created")`, `README.md`'s Troubleshooting log-line list (`:240-267`) needs the matching triage bullet. The README half cannot be written before the log line's final wording exists; every other line in that list was verified to match the code exactly and this one must too. **DONE (final pass).** Two bullets added to `README.md`'s Troubleshooting list against the exact shipped wording: one for `the application window could not be created` (naming the three real causes — no usable GL context, an RDP/remote session, a service account with no desktop — and why the log simply ends there), and a second for `startup failed` followed by `the error window could not be shown either`, which is a *different* path (`lib.rs::fatal`) rather than a continuation of the first, as a draft of this bullet had assumed.

## From the `src/watch.rs` agent

- [x] `name-002` — needs `src/app/session/mod.rs:81`, `src/actuator/mod.rs` (6 test sites), `src/ui/mod.rs:469` — rename `WatchGate::halt_requested()` to `next_halt()` (or `wait_for_halt()`); it reads as a cheap `bool` predicate but is an `async` wait returning the cause, and it is the first `biased` arm of the session `select!`. Not renamed here: 10 of the 17 call sites are outside `src/watch.rs`, so a rename in isolation breaks the build. The declaration and the 7 in-file test call sites are ready to move in one `sed` once a pass owns all four files. **DONE (api-shape pass).** Renamed to `next_halt()` at all 17 sites across `watch.rs`, `actuator/mod.rs`, `app/session/mod.rs` and `ui/mod.rs`; the doc comment now says why it is `next_` and not `is_` (it parks the loop, it does not test).

## From the `src/uplink/` agent (`websocket.rs`, `protocol.rs`, `mod.rs`)

- [x] `obs-001` — needs `src/app/mod.rs` — the P0 leak is **closed**: `websocket.rs` no longer logs `url` in any form (both lines now carry `attempt` instead), and `UplinkEvent::LinkDown`'s reason strings were checked variant-by-variant against tungstenite 0.29 to confirm none embeds the URL. What remains is the *nicer* shape the report asked for: make `redacted_server_url` (`src/app/mod.rs:262`) `pub(crate)` and pass `redacted_server_url(&config.server_url)` into `uplink::run` beside the dial string, so the per-attempt lines can name the server again in redacted form. Not done here because it changes `uplink::run`'s signature and the only call site is `src/app/mod.rs:352`; writing a second redactor inside `uplink/` was explicitly ruled out. **Progress (api-shape pass):** `app::redacted_server_url` no longer exists — `api-002`'s field flip landed, so the redactor is `config::ServerUrl::redacted()`. The remaining shape is therefore `uplink::run(url: ServerUrl, …)` (one call site, `src/app/mod.rs`, currently `config.server_url.as_str().to_owned()`), with `websocket.rs` using `url.as_str()` to dial and `url.redacted()` in the per-attempt lines. Note the three `run_with_connector` test call sites pass `"ws://test.invalid"`, which `ServerUrl::parse` rejects as non-loopback cleartext — they would need `wss://`. **DONE (final pass).** `uplink::run`/`run_with_connector` take `url: ServerUrl`; the dial string is reachable only through `url.as_str()`, used once, at the connector call. Every log line in `websocket.rs` carries `server = %url`, which resolves through `ServerUrl`'s `Display` — the redacted authority — so the leak is unrepresentable rather than avoided, and a line added there later cannot reintroduce it. A `link` span (`server`, `attempt`) wraps `pump`, so the events emitted *inside* the connection carry it too (that is `obs-011`'s boundary 3). The three `run_with_connector` test call sites became `wss://test.invalid`, exactly as this line predicted `ServerUrl::parse` would require, behind a `test_url()` helper that says why.
- [x] `async-003` — needs `src/app/mod.rs:348-358` — the uplink is still the one worker with no cooperative shutdown path. The in-scope half cannot stand alone: adding `shutdown: watch::Receiver<bool>` to `run`/`run_with_connector` breaks the single call site immediately. Ready-made shape for whoever owns both files: add the parameter, pass `shutdown_rx.clone()` at the spawn site, and race it `biased` against the (now bounded) `tokio::time::timeout(CONNECT_TIMEOUT, connect(...))` at `websocket.rs:130` and around the `SEND_TIMEOUT` send in `pump`'s writer half. `async-001` has already removed the *unbounded* window, so this is now a latency fix (≤15 s worst case) rather than a correctness one. **DONE (final pass); the ready-made shape was right but incomplete.** `run`/`run_with_connector` take `shutdown: watch::Receiver<bool>` and the spawn site passes `shutdown_rx.clone()`. **Three** windows are raced, not two: the `CONNECT_TIMEOUT` handshake, the connected `pump` (a third `stopping` arm beside writer and reader, so a `SEND_TIMEOUT` write *and* an idle reader are both covered), and — the longest, which this line did not name — `drain_until`'s backoff wait, which climbs to `reconnect.max_ms`. A `wait_for_shutdown` helper re-reads the current value before awaiting `changed()`, because `changed()` only fires on a transition and the signal may already be set; `is_stopping` is a sync `fn` so the non-`Send` `watch::Ref` cannot cross an await and make the task un-spawnable. Three new tests, one per window.
- [x] `serde-002` — needs `src/app/session/mod.rs:190-212` (`heartbeat`) — the silence is fixed at the decode boundary instead: `websocket::forward` now warns once per connection when a payload parses into `ServerMessage::Unknown`, so "server talking, client not understanding" is distinguishable from "server mute" in the log. The report's second half — carrying the unknown-tag *count* as a `heartbeat` field so the fourth failure mode is visible in the periodic line too — still needs `session/mod.rs`. `on_message`'s `ServerMessage::Ack | ServerMessage::Unknown => {}` arm can stay as it is; the log line is emitted upstream now. **ALREADY DONE when triaged (final pass).** `session_loop` keeps an `unknown_messages` counter, increments it on `ServerMessage::Unknown`, and passes it to `heartbeat`, whose doc says it is what separates "server mute" from "server talking, client not understanding". Ticked so a later pass does not re-derive it.

## From the `src/domain/` agent (`mod.rs`, `filter.rs`, `shop.rs`, `control/{mod,dedup,watchdog,tests}.rs`)

- [x] `api-001` — needs `src/app/session/tests.rs` (`:581`, `:698`, `:746`, `:863`, `:1060`, `:1161`) and `examples/ui_preview.rs:159` — `#[must_use = "these actions are the decision — dropping them loses the refresh or the buy"]` on `Controller::handle` is the right fix and is **not applied**: seven call sites drop the `Vec<Action>` today, all outside `src/domain/`, so under CI's `-D warnings` the attribute alone reddens the build. Landing it means adding `let _ = …` (or an assertion) at each of those seven lines in the same commit as the attribute. The three in-scope drops in `control/tests.rs` are already assertions. **DONE (api-shape pass).** The attribute is on `Controller::handle`, with the note that `must_use_candidate` structurally cannot flag a `&mut self` method. The drop-site count was **111**, not 7: 87 in `domain/control/tests.rs` plus 24 in the `render.rs`/`ui/*` test modules that neither the domain nor the app agent could see. All are `let _ = …` now.
- [x] `api-005` — needs `src/app/mod.rs:292`, `src/ui/editor/mod.rs:629`, `src/render.rs:70` — the `HuntFilter` newtype cannot land from inside the domain. `Filter::restricted(self) -> Result<HuntFilter, Filter>` consumes by value, but `on_start` proves the invariant about `self.filter` (a borrow), so making it compiler-checked means `Controller` storing a `HuntFilter` while armed — which changes `Controller::new`'s signature, whose callers are `src/app/mod.rs` plus both test suites. The report's own payoff (deleting `app/mod.rs:292`'s duplicate check in favour of the domain's `Action::Refused`) is entirely outside this file set. The two in-domain call sites (`control/mod.rs:373`, `:402`) are unchanged and correct; nothing was half-done. **NOT APPLIED, deliberately — see the api-shape pass's report.** Two reasons. (1) The newtype does not remove a state, it adds one: the GUI path must be able to *hold* an unrestricted draft (that is why it boots where the console fails fast), so `Controller` would need both a draft `Filter` and an armed `HuntFilter`, and every `self.filter` reader (matching, checklist, `filter()`, the view projection) has to pick a side. (2) The report's own cited payoff — deleting `app/mod.rs`'s pre-flight check in favour of the domain's `Action::Refused` — is a behaviour change the code argues against in the doc comment two lines above it: the console build has no filter editor, so booting into a permanent refusal is worse than an `Error::Config` at startup. The rule itself is not duplicated — `Filter::is_unrestricted` is the single implementation; only the *reaction* differs by build, on purpose.
- [x] `own-003` — needs `src/app/mod.rs:224`, `src/ui/mod.rs:115,308`, `src/ui/view.rs:81`, `src/ui/editor/mod.rs:54,80` — adding `Copy` to `Limits` (40 bytes, every field `Copy`) is one word, but it turns those six `.clone()` calls into `clippy::clone_on_copy` diagnostics, i.e. a red build under `-D warnings`. Derive and the six deletions must land together. **DONE (api-shape pass).** `Copy` derived with the reason at the type, and 11 `clone_on_copy` sites removed (7 in the lib, 4 in test modules — more than the 6 measured, because two landed after this line was written).
- [x] `name-003` (one of four sites) — needs `src/app/mod.rs:2153,2162,2167` — rename `Controller::recovery_enabled` → `is_recovery_enabled`; it sits one method below `enable_recovery(&mut self)` and differs by one word from a mutator. The three callers are `app/mod.rs` tests, so the rename breaks the build in isolation. `duration_elapsed` → `has_duration_elapsed` **was** applied (all three sites in-scope). **DONE (api-shape pass).** Renamed to `is_recovery_enabled` at the declaration and the three `app/mod.rs` test callers.
- [x] `const-004` (watchdog half) — needs `src/app/session/tests.rs:1187,1219,1291,1397` — `EXPECT_SNAPSHOT_MS`/`EXPECT_PURCHASE_MS` are still private to `control::watchdog`, so both test suites hard-code `10_001`/`20_001`/`30_001` (~24 sites in `control/tests.rs`, 4 in `session/tests.rs`). Reaching the sibling suite needs `pub(crate)`, not `pub(super)`, so the two halves want one pass. Left alone rather than renaming 24 literals in a file whose real diff should stay readable. **DONE (api-shape pass), by a smaller shape than the item assumed.** Rather than exporting both windows and re-spelling 34 literals as arithmetic, `watchdog` gained a `#[cfg(test)] pub(crate) const fn past_rung(rung)` — the *derived* deadline, re-exported as `domain::control::past_rung` — so both suites read `tick(past_rung(2))` and neither has to know whether the outstanding expectation is a snapshot or a purchase. 20 sites in `control/tests.rs`, 11 in `session/tests.rs`, plus the two `max_duration_ms: Some(10_001)` in `timeout_beats_the_watchdog_on_the_same_tick`, which are now `Some(past_rung(1))` because that coincidence *is* the test. `EXPECT_SNAPSHOT_MS`/`EXPECT_PURCHASE_MS` stay private, and a `const _: () = assert!` pins the equality `past_rung` collapses.
- [x] `type-005` (wire half) — needs `src/uplink/protocol.rs:29-31` — the *re-derived* sentinel is fixed: `control/mod.rs:568`'s `item != 0` now calls `shop::catalog_id`, the single interpreter. Making the sentinel unrepresentable (`id: Option<NonZeroU32>` via `deserialize_with`, `PurchaseNotice::item` likewise) still needs the uplink type, and pairs with `type-004`'s `CatalogId`. **DONE (final pass), together with `type-004`'s `CatalogId`.** `shop::CatalogId(NonZeroU32)` with `#[serde(transparent)]`, one `deserialize_with` (`shop::optional_catalog_id`) folding absent / `null` / `0` to `None`, and both wire fields flipped: `ShopItem::id: Option<CatalogId>` and `PurchaseNotice::item: Option<CatalogId>`. `checklist`, `bought`, `BuyTarget::id` and `Event::Purchase::item` speak the type too, so an id can no longer be compared against a price or a balance. Both interpreters are **gone** — the free `shop::catalog_id` and the `ShopItem::catalog_id` accessor — which is what removes the re-derivation the report caught (`on_purchase`'s `item != 0`) instead of re-forbidding it. ~90 call sites; each test module got a `cid()` helper that panics on `0`. `type-004`'s `Gold`/`Crystals` half is **not** taken — see its own line.
- [x] `num-001` — needs `src/config.rs:344-424` — a `nan`/`inf` `required_substats[].min` is a legal TOML parse and makes the GUI's Apply dirty-check permanently dirty (derived `PartialEq` over `Option<f64>`) while `matches` can never satisfy `value >= min`. The report's fix is a finiteness clause in `Config::validate`, beside the `named_ranges` loop; `filter.rs` only supplies the type. `min_grade`'s sibling defect **was** fixed in-domain (see below). **ALREADY DONE when triaged (final pass).** `Config::validate` carries the finiteness clause over `required_substats[].min`, with `a_non_finite_substat_threshold_is_rejected` covering `nan`/`inf`/`-inf`/`-nan`.
- [x] `serde-003` (test twin) — needs `src/config/persist.rs:548-577` — the `skip_serializing_if` attributes are applied and covered by a new `inert_filter_keys_are_not_serialized` test in `filter.rs`. What remains is the report's request to extend `inert_timing_ranges_are_not_written` with a `Filter` twin so the round-trip is pinned at the layer that actually writes the file, plus the note that `required_substats` persists as an inline table rather than the `[[filter.required_substats]]` form `config.example.toml` documents. **ALREADY DONE when triaged (final pass).** `inert_filter_keys_are_not_written` sits in `persist.rs` beside its `Timings` twin, asserting the four inert keys are absent *and* that the omission round-trips through `Config`; the inline-table-versus-`[[filter.required_substats]]` divergence is recorded as a house-style note at `a_persisted_substat_requirement_reloads`. Nothing left.
- [x] `doc-009` (second file) — needs `src/app/session/tests.rs:1` — `control/tests.rs` now opens with a `//!` header naming what it covers and in what order; the crate's other 1423-line test submodule still opens with `use`. **DONE (already landed).** `src/app/session/tests.rs` opens with a five-point `//!` header in module-declaration order plus a fixture-placement note; nothing left to do.

## From the `src/stream.rs` agent

- [x] `api-007` — needs `src/app/mod.rs:1328,1361,1407` — `PipelineBudget::with_test_limits(global, capture, reassembly, outbound)` still takes four positional `usize`s. The named struct it should take (`BudgetLimits`, `stream.rs:46`, `Clone + Copy`) already exists and `with_limits` already takes it, so the edit inside `stream.rs` is one line — but all three call sites are `app/mod.rs` tests, and they are exactly the seams that pin the byte-budget guarantees, so the signature change and the three call-site rewrites must land together. Not half-done here. **DONE (api-shape pass).** `with_test_limits(BudgetLimits { … })`; `BudgetLimits` and its fields are `pub(crate)` (the constructor stays `#[cfg(test)]`, so no production path can build an arbitrary budget). Six call sites, not three — `uplink/websocket.rs:570,630,755` had the same four positional `usize`s.
- [ ] `mem-002` (preferred half) — needs `Cargo.toml` — landed as `Vec::with_capacity(1)` for `HalfStream::push`'s out-vec, which is the report's dependency-free fallback: it turns a 192-byte four-slot allocation per in-order packet into an exact 48-byte one, at the cost of one slot on the paths that deliver nothing. The report's first choice, `SmallVec<[BudgetedChunk; 1]>` for `out` and both `Chunks` variants, needs a `smallvec` dependency plus a change to `forward_chunks`' parameter type in `src/app/mod.rs:828`. **DECLINED (final pass), with the reasoning moved to the site rather than left here.** `SmallVec<[BudgetedChunk; 1]>` would inline 48 bytes into `HalfOutcome` *and* `ReassemblyOutcome`, both returned by value up two frames per packet — trading a malloc for a memcpy of the same order, with no measurement either way, for the price of a dependency. Nothing in this crate has been profiled (`Cargo.toml`'s `[profile.release]` says so at length about `lto`), and `mem-smallvec` itself asks for a profile first. The `Vec::with_capacity(1)` keeps the whole win that *is* provable from the numbers in the report.
- [x] `test-007` (`stream.rs` half) — needs `Cargo.toml` (+ `Cargo.lock`) — `proptest` is not a dev-dependency, so no property test could be added for `seq_diff` (wrap-antisymmetry) or for `Reassembler::push_budgeted` (arbitrary arrival permutations reassemble to the same byte stream). `initial_anchor_all_six_permutations_keep_the_immediate_suffix` is still the hand-enumerated 3! stand-in the report flagged. `seq_diff` is now `const fn`, which a property test can also exercise at compile time. **DONE (final pass), without the dependency — see the `test-007` verdict under the capture agent.** `every_arrival_order_yields_the_immediate_suffix_of_the_stream` **exhausts** 6! = 720 arrival orders across four origins (2 880 cases), two of which straddle the `u32` wrap, and asserts both halves of the documented rule: what comes out is a suffix of the original stream, and it starts exactly at the first segment to arrive. Exhaustion is strictly stronger than sampling, and it retires the hand-enumerated 3! table as the *general* proof (the table stays as the readable n=3 case). `seq_diff` got `seq_diff_is_antisymmetric_and_wrap_relative` over a lattice of eight bases including both sides of the half-space, plus the stated `i32::MIN` edge where "ahead" and "behind" are the same answer.
- [x] `lint-006` (progress, not a new ask) — `src/stream.rs:421,423` are **done**: both now read `size_of::<…>()` with no `std::mem::` prefix. Four sites remain (`src/actuator/win.rs:502`, `src/capture/mod.rs:80,82`, `src/migrate.rs:261`) before `unused_qualifications = "warn"` can go into `[lints.rust]`. **DONE (verified by the final pass).** All six sites are gone and `unused_qualifications = "warn"` is in `[lints.rust]` at 0 on every lane — including the 21 test-harness sites a later measurement on the capture agent's line found, which this line's "four remain" count predates.

## From the `src/config.rs` + `src/config/persist.rs` agent

- [x] `api-002` / `obs-013` — needs `src/app/mod.rs:258-267,352,412,2223-2238` and `src/uplink/websocket.rs:74` — **the boundary type is landed**: `config::ServerUrl` (`parse`/`as_str`/`redacted`, a `Debug`/`Display` that print only `scheme://host[:port]`, and `TryFrom<String>` as the `#[serde(try_from)]` hook), and `Config::validate` now goes through it, so the scheme+authority rule has one implementation instead of one here and one in the log redactor. What remains is flipping the field: `pub server_url: ServerUrl` + `#[serde(try_from = "String")]`, then `config.server_url.as_str().to_owned()` at `app/mod.rs:352` (or better, `uplink::run(url: ServerUrl, …)`), `config.server_url.redacted()` at `:412`, and **delete `app::redacted_server_url` (`:258-267`) and its test (`:2223-2238`)** — it is now a duplicate, and its doc comment (*"`Config::validate` accepts any `wss://` URL without inspecting either, so the redaction lives here rather than at parse time"*) is stale as written. Not done here because `Config` derives `Clone`, so `ServerUrl` must be `Clone`, so `config.server_url.clone()` at `:352` returns the newtype and the flip reddens the build in isolation. The same flip lets `Config`'s hand-written `Debug` (added for `obs-013`, `config.rs:79-113`) go back to `#[derive(Debug)]`. **DONE (api-shape pass).** `pub server_url: ServerUrl`, `#[serde(try_from = "String")]` on the newtype itself (a container attribute, not a field one), `Config` back to `#[derive(Debug)]`, `Config::default()` through `ServerUrl::parse` with a named `DEFAULT_SERVER_URL`, and the `server_url` clause deleted from `Config::validate` (the type *is* the check now — the comment there says so). `app::redacted_server_url` and its test are gone; the `relay started` line says `config.server_url.redacted()`. The 13 cleartext-rule tests now call `ServerUrl::parse` directly instead of building a whole `Config` and calling `validate()`, because that is where the rule lives; every input string is unchanged, both userinfo bypasses included. `uplink::run` still takes a `String` (`config.server_url.as_str().to_owned()` at the call site) — widening it to `ServerUrl` is `obs-001`'s remaining half and wants the redacted form threaded in at the same time.
- [x] `num-001` (GUI half) — needs `src/ui/editor/mod.rs:623` — the parse-time half is closed (`Config::validate` now rejects a non-finite `required_substats[].min`, with `a_non_finite_substat_threshold_is_rejected` covering `nan`/`inf`/`-inf`/`-nan`). What `num-float-compare` still asks for is one line above `let dirty_filter = editor.filter != editor.applied_filter;` saying the comparison is deliberately bit-exact — it is change detection since the last write, not a numeric test. **ALREADY DONE when triaged (final pass).** The comment above `dirty_filter` states that the comparison is deliberately bit-exact — change detection since the last write, not a numeric test, so an epsilon would make a real edit invisible — and names the one thing exactness cannot survive (a non-finite `min`) together with why no `Filter` reaching there carries one.
- [x] `serde-005` (the structural half) — needs `src/actuator/plan.rs:184-215` and `src/ui/editor/timing_meter.rs:131`, `:221-232` — the *second boundary* is closed: the timing loop moved out of `Config::validate` into `pub(crate) config::validate_timings`, which `persist::write_sections` now calls for `Section::Timings`, so the GUI can no longer write a `config.toml` the next launch refuses (`a_timing_range_the_loader_would_refuse_is_never_written`). The report's real fix — `DelayRange` carrying `min_ms <= max_ms <= MAX_TIMING_MS` by construction via `try_from`, which then collapses the four `max_ms.max(min_ms)` defences — still needs `plan.rs`, and moves `MAX_TIMING_MS` down a layer. **The two stale comments are still stale and are not in my files:** `timing_meter.rs:131` and the test comment at `:221-232` both say *"`[actuator.timings]` is not validated at load"*, which stopped being true when `MAX_TIMING_MS` was added; the accurate claim is "`DelayRange` itself carries no invariant". **DONE (final pass), the report's real fix, with two corrections to it.** `plan::DelayRange` has private fields and carries `min_ms <= max_ms <= plan::MAX_TIMING_MS` by construction: `try_new` (fallible, with a `DelayRangeError` whose two messages keep the old prose about what the value *would have done*), `ceiling` (infallible — for the presets and the GUI drag), `set_max_ms` (the drag's one mutator, which brings a config floor down with the ceiling), and `#[serde(try_from = "RawDelayRange")]` so `config.toml` is no exception. `MAX_TIMING_MS` moved down into `plan` with the check. **Correction 1:** `config::validate_timings` was not kept as defence — it was **deleted**, with `persist::write_sections`' call to it and the `Error::Config` clause in `Config::validate`, because a function that structurally cannot fail is worse than none; a comment where it stood records what it guarded and why the type replaced it. **Correction 2:** the timing errors are `Error::ConfigParse` now, so the tests assert through `Error::report()` (the chain walk `err-008` added) against the `toml` excerpt, which quotes the offending line — the key is named by the excerpt rather than by hand. `draw`'s `checked_add`/`saturating_*` and the four `max_ms.max(min_ms)` defences in `ui/editor` and `timing_meter` collapse to plain arithmetic, and **both stale comments this line flagged are corrected**, not deleted.
- [x] `type-010` (the two halves outside config) — needs `src/app/mod.rs:1108`, `src/main.rs:204`, `src/capture/pcap.rs:493,537,551,602,647`, `src/capture/ip.rs:20,54` for `game_port: NonZeroU16`; and `src/domain/filter.rs:23` + `src/ui/editor/mod.rs` for a closed kind enum so `ItemKind::Unknown` is unrepresentable in `Filter::kinds` rather than rejected after the fact (this is the invariant that already shipped as a bug). Invariant 1 (`server_url`) is covered by the `api-002` line above and invariant 3 (`DelayRange`) by the `serde-005` line; the `game_port == 0` and `kinds` clauses in `Config::validate` are unchanged and still the only enforcement. **Update (api-shape pass):** invariant 1 is now genuinely closed — `Config::server_url` *is* a `ServerUrl` and `Config::validate` has no `server_url` clause left. The `game_port == 0` and `kinds` clauses are still the only enforcement for the other two, unchanged. **DONE (final pass) for `game_port`; the closed-kind enum DECLINED, with a different fix in its place.** `Config::game_port` is a `NonZeroU16` (`DEFAULT_GAME_PORT` too, so its `expect` is a compile-time check), threaded through `PcapSource::open`, the BPF filter string and `parse_segment`/`segment_span`; the `game_port == 0` clause is gone from `Config::validate` and a new test pins that serde refuses `0` and that `1` still parses — the old clause had **no test at all**. For `kinds`: a closed `HuntKind { Equipment, Hero, Token }` was written, compiled, and **reverted**. `ItemKind::Unknown` is a *meaningful* criterion in the domain — `Filter::matches` compares it against a kind the wire really reported, and `Filter::matching_default_items` (the only restricted-yet-matching fixture **30** tests have, because a default `ShopItem` has kind `Unknown`) is built on exactly it; with `HuntKind` there is no filter at all that is both restricted and matches a default item. It is ambiguous only as *text*, where a typo and a deliberate "hunt the kind you cannot name" are the same six bytes. So the rule moved to the boundary where the ambiguity is: `filter::hunt_kinds`, a `deserialize_with` in the idiom `grade_floor` in that same file already uses, which names the value it could not read *and* the three that are legal — something the old clause could not do, because `serde(other)` had already swallowed the typo. It also covers the boundary the old clause never did: the Setup tab writes `[filter]` through `persist::save` with no `Config` in the path, which is how the shipped checkbox got a `kinds = ["unknown"]` onto disk. The checkbox row is driven by `filter::HUNTABLE_KINDS`, so a fourth box cannot appear without a fourth kind existing.
- [x] `api-008` (journal half) — needs `src/ui/mod.rs:229-236` — `Section` now derives `Debug, Clone, PartialEq`, so `"config.toml not saved: {err}"` can finally name which sections were being written (`persisted_sections(&delivered)` is right there), and `ui/mod.rs:433`'s `matches!(sections[0], Section::Limits(_))` can become an `assert_eq!`. **DONE (already landed).** `ui/mod.rs` already emits `config.toml not saved ({labels}): …` through `section_labels(&sections)`, warns with a `sections` field, and `only_setup_commands_become_persisted_sections` is already an `assert_eq!` against `vec![Section::Limits(Limits::default())]`.
- [x] `proj-003` (the evidence half) — needs nothing but a pass that owns both files — `src/lib.rs:100` now exposes `pub fn seed_config_if_missing`, so `config.rs`'s `bundled_example_config_parses_validates_and_is_restrictive` (`:1199`, the `std::fs::write(&path, text)` line) can call the real seeding function instead of re-implementing it, which is the exact payoff the finding cited. Left alone deliberately: it would make a test in my file depend on another agent's same-run addition, and a revert of that addition would break the build rather than a test. **DONE (final pass).** `bundled_example_config_parses_validates_and_is_restrictive` calls `crate::seed_config_if_missing(&path)` instead of re-implementing it with `fs::write(include_str!(…))`, and asserts the file it produced is the bundled text verbatim. The fixture's own `create_dir_all` went with it — the real seeder does that too, which was half the point of the finding.
- [ ] `proj-004` (config half) / `test-011` / `test-006` — needs the `proj-009` move first (already listed above, and it needs `Cargo.toml`) — `src/config.rs` is 434 production lines under 840 test lines; extracting `#[cfg(test)] mod tests;` is gated on settling `config.rs` → `config/mod.rs`, so both want one pass. The five filesystem-lifecycle tests `test-011` names (`save_then_load_round_trips_…`, `stripping_a_players_config_…`, `a_failed_strip_…`, `a_failed_save_…`, `stripping_a_missing_config_…`) and the hand-rolled `TempDir` guard (`config.rs:1212-1246`) move with it; `test-006` wants that guard replaced by a shared `tempfile` fixture, which needs `Cargo.toml` and `src/crash.rs`. **OUT OF SCOPE for the api-shape pass** — gated on `proj-009`'s module move, which is itself out of scope for this run. Still open, unchanged. **Still open, and one premise has changed (final pass).** `test-006`'s "a shared `tempfile` fixture" is off the table: `test-005` landed the opposite decision — `tempfile` is not a dependency of this crate at all any more, because dropping `egui_kittest`'s `snapshot` feature removed it from the resolution and `crash.rs` carries its own guard. If `test-006` is ever taken, the shared fixture has to be this crate's own — and `config.rs`'s `TempDir` is the one to promote, not `crash.rs`'s `TempFile`: it removes its directory on drop *including* when an assertion panics, and it mixes the pid with a process-local counter so parallel test binaries cannot collide. The rest of the item is unchanged and still gated on `proj-009`.

## From the startup/observability agent (`src/main.rs`, `src/lib.rs`, `src/crash.rs`, `src/migrate.rs`, `src/error.rs`)

- [x] `obs-002` (journal half) — needs `src/app/mod.rs` or `src/ui/mod.rs` — the log-file half is **done**: `install_logging` now walks a two-candidate ladder (`%LOCALAPPDATA%\<app>\logs` then `%TEMP%\<app>\logs`, `lib.rs::log_dirs`) and returns a `LogSetup` whose `report()` emits `error!(dir, error = ?err)` per refusal, a `warn!` when the fallback was used, and one `error!` when there is no log file at all. Smoke-tested against an unwritable `%LOCALAPPDATA%`: the fallback file is created and the OS error code (183) is in the record. What remains is the report's second ask — a **journal** line so the banner tells the player "there is no log file", which cannot be written from `lib.rs`'s startup path because `EventLog` is created later, inside `app::setup`. Shape: pass `LogSetup::destination()` (already `pub`) into `app::setup` and `journal.emit` one line when it is `None`. **DONE (final pass), from `main.rs` rather than by widening `app::setup`.** `run_mode` takes `log_file: Option<&Path>` (`log_setup.destination()`), and the GUI arm journals one `emit_at(WARN, …)` line right after `app::setup` when it is `None`. Deliberately *not* threaded into `app::setup`: it is a property of this process's startup, not of a session, and the console arm needs nothing — when every candidate refuses, `install_logging` falls back to a stdout subscriber, and that is the lane with a real terminal, so "there is no log file" is already on screen there. The `_log_file` parameter on the console arm carries that reason.
- [x] `obs-004` (README half) — the log line's **final wording is now fixed**: `tracing::error!(error = %err, "the application window could not be created")` at `src/main.rs`'s GUI `run_mode` `Err` arm. `README.md`'s Troubleshooting list (`:240-267`) can take its triage bullet against that exact text. The sibling `eprintln!("error window failed: …")` inside `fatal` also logs now: `error!(error = %err, "the error window could not be shown either")`. **DONE (final pass).** See the manifest agent's `obs-004` line above for what landed.
- [x] `conv-004` — needs `src/app/mod.rs:590` — dropping `#[from]` from `Error::Io` is a one-word edit in `src/error.rs`, but the only `?` that uses the impl is `std::thread::Builder::spawn` in `spawn_capture_with_budget`, so the attribute cannot come off without that call site becoming an explicit `map_err(|source| Error::Capture(format!("spawning the capture thread: {source}")))`. `error.rs`'s module header now states the convention the change would complete. **DONE (api-shape pass), by deletion rather than by dropping the attribute.** `Error::Io` had zero producers left, so the whole variant is gone and a comment in its place says why re-adding a blanket `#[from] std::io::Error` would re-open the door. Same edit closes `err-004`.
- [x] `err-008` — needs `src/app/mod.rs:633` — the report's preferred direction (a) (drop `{source}` from `ConfigRead`/`ConfigWrite`'s `Display` and print the chain with `{err:#}` at the two report sites) still needs `app::supervise`. Not half-done: `src/config/persist.rs:698` asserts `source()` is present, so the attributes must stay, and dropping `{source}` from `Display` while `supervise` still prints a bare `{err}` would *lose* the cause at a site outside this file set. What **was** done: the convention is now written down at the top of `src/error.rs` ("`Display` carries everything"), which is the half of the fix the report asked for unconditionally. **DONE (api-shape pass), direction (a), and the convention flipped with it.** `{source}` / `{0}` dropped from all five cause-carrying variants (`ConfigParse`, `ConfigRead`, `ConfigWrite`, `ConfigReparse`, `ConfigSerialize`), and `Error::report()` — a `source()` walk joined with `": "` — added, because `{err:#}` does **not** walk a chain for a `thiserror` enum (that part of the report's fix does not work as written; a helper is required). Five report sites, not two: `main`'s `Invalid configuration` and `Fatal error` arms, `app::supervise`, `ui::App`'s `config.toml not saved` journal line, and `RetiredKeys::NotRewritten`. `#[source]` attributes all stay, so `config/persist.rs`'s `source()` assertion is untouched. Three new tests in `error.rs` pin one-layer-`Display` / full-chain-`report` and that no variant repeats its own source.
- [ ] `type-008` — needs `src/app/mod.rs:622-636`, `src/ui/mod.rs:35,147-150` — the `(String, bool)` session outcome is untouched; `main.rs` still splits it into a `SessionErrorSlot` and an `Arc<AtomicBool>`. The exit-code half is now a testable pure function (`lib.rs::exit_code(window_closed_cleanly, session_failed)`) used by both `run_mode` arms, so when `SessionOutcome` lands the only change on this side is `failed.load(…)` → `outcome.failed()`.
- [x] `async-004` (`supervise` half) — needs `src/app/mod.rs:626` — `main.rs`'s half is **done** (the `JoinHandle` is borrowed through `timeout`, then `abort()`ed and drained, instead of detached). `app::supervise`'s inner `tokio::spawn(session).await` still detaches on cancellation; the report's fix is a single-element `JoinSet` for abort-on-drop. **ALREADY DONE when triaged (final pass).** `app::supervise` uses a single-element `tokio::task::JoinSet`, which aborts on drop, and its doc comment states the detach-versus-abort reasoning the report gave. Nothing left.
- [ ] `proj-003` (follow-up) — needs `src/config.rs:957` — `seed_config_if_missing`, `config_path`, `log_dirs`, `install_logging`, the retired-key decision table (`RetiredKeys`), `fatal` and `exit_code` all moved from `src/main.rs` to `src/lib.rs` and are now `pub`, with 10 new tests. `bundled_example_config_parses_validates_and_is_restrictive` can therefore drop its hand-rolled `std::fs::write(&path, include_str!(…))` and call `arkyve_refresh_shop::seed_config_if_missing(&path)` — the real writer, guard included. `main.rs` is 274 lines and holds only dispatch, the two `run_mode` arms and the `windows_subsystem` attribute (which must stay on the binary). Note for whoever finishes `proj-003`: the two `run_mode` arms were deliberately **not** moved — the user excluded structural file splits from this run, and they are not testable without a display.
- [x] `test-005` — **the `src/crash.rs` decision the manifest agent asked for: drop `snapshot`, and do not add `tempfile`.** `test-004` was fixed with a ~20-line `#[cfg(test)] struct TempFile` RAII guard inside `crash.rs`'s own test module, so nothing in this crate needs `tempfile` compiled. `Cargo.toml:95` can therefore become `features = ["wgpu"]` on its own, with only the `Cargo.lock` refresh as a prerequisite — option (b) of `test-004` is off the table. **HONOURED (final pass).** `snapshot` dropped, `tempfile` **not** added — `crash.rs`'s own `TempFile` guard is the only thing this crate needs. See the manifest agent's `test-005` line for the lock refresh and the lane verification.
- [x] `lint-009` / `lint-006` / `lint-004` (my share is done) — `redundant_clone`'s single site (`main.rs:307`) is gone, `migrate.rs`'s six `doc_markdown` sites and `migrate.rs:261`'s `std::mem::` prefix are gone, and `migrate.rs:183`/`:258`'s oversized `unsafe` blocks are split into one Win32 call each with its own `// SAFETY:` (so `multiple_unsafe_ops_per_block` is now clean in `migrate.rs`). Measured after the fact: `redundant_clone`, `needless_pass_by_value`, `clone_on_ref_ptr` and `multiple_unsafe_ops_per_block` are all at **0** across `main.rs`/`lib.rs`/`crash.rs`/`migrate.rs`/`error.rs`. What blocks each lint's `[lints]` entry is still the other files listed in the manifest agent's items above. **DONE (closed by the api-shape pass).** Everything this line was waiting on in other files is fixed and the corresponding `[lints]` entries have landed; see the `lint-004`, `lint-006`, `lint-007`, `lint-008` and `lint-009` notes above for what did and did not get enabled.
- [x] `obs-003` / `obs-006` — needs `src/journal.rs`, `src/actuator/mod.rs`, `src/app/session/mod.rs`, `src/ui/mod.rs` — unchanged and still the biggest remaining hole in the third theme: the journal is the only observability the actuator and session layers have, and it is single-level. Recording it here only because `.with_target(false)` (the aggravating factor named in `obs-003`, now in `lib.rs::install_logging` rather than `main.rs:97`) was **kept** on purpose: it is what makes the file readable, and the fix belongs at the emit sites, not at the subscriber. **RESOLVED (final pass) — and the two `src/ui/mod.rs` sites are *not* the mistake this line expected.** The actuator's halt and abort lines record at `error`/`warn` through `emit_at`, and the session loop's link-down and abort lines likewise. The two `ui/mod.rs` `push` calls were re-read one by one and are **correct as written**: each already has its file half beside it, and a better one than `emit_at` could give — `persist_sections`' is a `tracing::warn!` carrying `error`, `path` and `sections` as *structured fields*, which `emit_at` would replace with one prose string, and `deliver_command`'s is a `tracing::debug!` at a level `emit_at` cannot express (it offers INFO/WARN/ERROR only). A comment at each now says so, so the next audit does not re-flag them.

## From the `src/capture/` agent (`pcap.rs`, `ip.rs`, `mod.rs`)

- [x] `mem-001` / `own-001` — **done, but it moves a number another file owns.** `parse_segment` now takes the frame `Vec<u8>` **by value** and trims it in place (`truncate` + `drain`), so the packet path is one copy instead of two. The consequence for `src/stream.rs`: `PipelineBudget::admit_capture` charges `segment.payload.capacity()`, which is now the *whole frame's* — payload plus IP and TCP headers plus any Ethernet padding, so ~40-60 bytes more per admitted packet against `CAPTURE_STAGE_BYTES`. That is deliberate and is the honest number (it is the memory actually retained, and it makes the one per-packet buffer the budget used to be blind to visible to it), and it is *self-consistent*: `PayloadLease` records the charged bytes and `BudgetedChunk::capacity()` returns `lease.bytes`, never a re-read of `Vec::capacity()`, so nothing double-counts. No `stream.rs` test moved (they all build their own `Segment`s), and `Segment` is still 96 bytes so `capture/mod.rs`'s canary is unchanged. Nothing to finish — recorded so the `stream.rs` owner is not surprised, and so a later pass can decide whether `CAPTURE_STAGE_BYTES` wants re-baselining rather than discovering the change from a graph. **CLOSED (final pass), with the decision this line asked a later pass to make.** `CAPTURE_STAGE_BYTES` is **not** re-baselined, and the reasoning now sits in `admit_capture`'s doc rather than only here: the frame overhead is ~50% on a minimum-size segment and ~4% on a full-MTU one, so the quota holds proportionally fewer packets — but the quota is a *memory* bound, not a packet-count target, and 8 MiB is the memory it was chosen to bound. A new number would be an unmeasured claim replacing a measured byte count in a crate that has never been profiled. The one relation that could have broken is checked at compile time and still holds with three orders of magnitude to spare (`INITIAL_ANCHOR_MAX_BYTES` is 256 KiB).
- [x] `test-007` (`parse_segment` half) — needs `Cargo.toml` + `Cargo.lock` — the report's `parse_segment_never_panics` property test wants `proptest = "1"` as a dev-dependency, which is a dependency-graph change nobody owns in this wave (same reason as `test-005`). The function is now `parse_segment(Vec<u8>, u16)`, so the sketch in `20-test.md:388` becomes `let _ = parse_segment(bytes, port);` with `bytes` moved in directly. **DONE (final pass), and `proptest` DECLINED on the merits.** The dependency was weighed, not skipped: it costs roughly eight test-only crates on nine `--locked` lanes plus a committed `proptest-regressions/` file this repository has no convention for, and for these two functions the shrinking it buys is worth little — `to_screen` is piecewise linear, so all of its interesting behaviour is at boundaries a lattice names *deliberately* and a uniform generator reaches by luck, and `parse_segment`'s whole contract is "`None` or a valid `Segment`, never panics", where there is no counterexample to minimise, only a seed to re-run. What landed instead: `to_screen_maps_every_shape_inside_the_client_area`, a 1 152-case lattice over heights, widths across all three aspect regimes, origins and anchors, asserting the three properties the executor relies on and no example test stated (in-bounds, symmetric pillarbox bars, monotone in design-x); and `parse_segment_is_total_on_hostile_bytes`, two *seeded* sweeps — garbage at every length to 80 bytes, 32 single-byte mutations at every offset of a valid packet, and every truncation — reproducible from the test source with no regressions file. The mutation sweep carries a tripwire asserting it still parses >100 mutations, so a fixture change cannot make it silently inert.
- [ ] `proj-005` — needs new files (`src/capture/pcap/{mod,ffi,link,devices,worker}.rs`) — **not done**, deliberately: the split creates files outside the set I was given, and the report's own cost note (widening `Handle`'s fields to `pub(super)` widens the audience of the `unsafe impl Send` argument from one file to three) makes it a change that wants its own review, not a rider on nine other findings. The banners it splits on are all still exactly where the report found them; the file is 1234 lines now (1039 production). **OUT OF SCOPE for the api-shape pass** — the user excluded structural file splits from this run, and the report's own cost note (widening `Handle`'s fields to `pub(super)` widens the audience of the `unsafe impl Send` argument from one file to three) makes it a change wanting its own review. Still open, unchanged.
- [x] `lint-004` — **the prerequisite is complete: `clippy::multiple_unsafe_ops_per_block` now measures 0 across `--all-targets --all-features`.** `pcap.rs:239` (the 13-op block) is split — the `sym!` macro is hoisted out of the `unsafe` and each `lib.get` carries its own one-op block plus `// SAFETY:` — and `:768` and `:789` are one Win32 call per block each; the `migrate.rs` half is reported done by the startup agent. So the `multiple_unsafe_ops_per_block = "warn"` line can land in `Cargo.toml`'s `[lints.clippy]` with nothing else to fix first. **DONE (verified by the final pass).** `multiple_unsafe_ops_per_block = "warn"` has been in `[lints.clippy]` since the api-shape pass and measures 0 on all six lanes.
- [x] `lint-006` (capture half) — needs `Cargo.toml` and `src/actuator/win.rs:502` — `capture/mod.rs:80,82` now say `size_of::<…>()` unqualified. Measured after the fact: with `-W unused_qualifications --all-targets` the crate is at **22** sites, of which exactly one (`actuator/win.rs:502`) is production code; the other 21 are `std::mem::size_of`/`std::time::Duration`-style spellings in test modules (`app/mod.rs`, `config.rs`, `app/session/tests.rs`, `ui/mod.rs`, `uplink/protocol.rs`). Since a `[lints.rust]` table applies to the test harness too, enabling the lint needs those 21 as well — which is more than the "6 live sites" the original item assumed. **DONE (verified by the final pass).** `unused_qualifications = "warn"` is in `[lints.rust]`, including the 21 test-harness sites this line's later measurement found, and measures 0 on all six lanes.
- [x] `lint-009` (`doc_markdown`, capture half) — needs `Cargo.toml` and the remaining sites — `pcap.rs`'s six are backticked (`WinDivert` ×2, `QinQ`, `EtherType` ×3). Measured after the fact: **9** left, in `build.rs:35`, `actuator/plan.rs:385`, `actuator/win.rs:350,511,1352`, `config/persist.rs:390`, `domain/control/mod.rs:282`, `domain/filter.rs:19`, `examples/ui_preview.rs:10`. **DONE (final pass).** All nine remaining sites are fixed and `doc_markdown = "warn"` is in `[lints.clippy]` at 0 — see the manifest agent's `lint-009` line.
- [ ] `lint-008` (the `pcap.rs` site) — needs the `src/render.rs` direction decision — `pcap.rs`'s single `redundant_pub_crate` hit is `pub(crate) struct PcapStop` / `pub(crate) fn open` inside the private `mod pcap`. Left alone on purpose: it is the same visibility question as `render.rs`'s 13, and demoting it in isolation would answer that question for the whole crate from the wrong file.
- [x] `obs-011` (boundary 2 of 3) — **done, and the pattern is now in the tree to copy**: `capture_loop` enters `info_span!("adapter", device = %short_device_name(&handle.device))` as a guard (no `.await` in that function), and the four hand-repeated `device` fields — two exit lines plus `poll_drops`' `warn!` — are gone. Boundaries 1 (`app/mod.rs:488-505`, `SessionWorkers::spawn`, the one-line four-task win) and 3 (`uplink/websocket.rs`) are untouched and are where the remaining value is. **DONE (final pass).** Boundary 1 was already landed when triaged (`SessionWorkers::spawn` carries `.instrument(info_span!("worker", name))`). Boundary 3 landed as a `link` span (`server`, `attempt`) around `pump` via `.instrument()`, not an `.entered()` guard — `pump` awaits, and a guard held across an await is how a span leaks onto whatever the executor polls next. The span is what reaches the events emitted *inside* the connection (`send failed`, `send stalled`, `WebSocket read error`, `forward`'s two decode lines), which are the only ones in that module that did not already carry `attempt`.
- [x] `unsafe-001` (the two remaining callers' shape) — nothing to finish, recorded because it changed a rule the crate advertises: `cstr` is now `unsafe fn` with a `# Safety` section, so **the crate has two `unsafe fn`s** rather than the one `21-doc.md:11` counts, and the two error-buffer call sites went through the new safe `errbuf_text(&[c_char; PCAP_ERRBUF_SIZE])` instead. If a later pass re-runs that audit's `unsafe fn` census, the expected answer is 2 (`Wpcap::error_text`, `cstr`), both documented. **NOTHING TO FINISH — confirmed by the api-shape pass.** The `unsafe fn` census is still 2 (`Wpcap::error_text`, `cstr`), both documented; re-verified after splitting `actuator/win.rs:278`'s DPI block, which added no `unsafe fn`.

## From the `src/journal.rs` agent

- [x] `name-001` — needs `src/actuator/mod.rs` (14), `src/app/session/tests.rs` (18), `src/ui/mod.rs` (4), `src/app/mod.rs:2139` — rename `EventLog::entries()` → `to_entries()` per `name-to-expensive`. 37 of the 40 call sites are outside `src/journal.rs`, so the rename cannot land from here without breaking the tree while the `ui` agent is in it. All 37 are mechanical (`journal.entries()` → `journal.to_entries()`); nothing else about the call shape changes. Only two are production (`src/ui/mod.rs:117`, `:144`), the rest are test assertions, so one `sed -i 's/\.entries()/\.to_entries()/g'` over those four files plus the three in-file sites is the whole change. The declaration's doc comment already says what it costs. **DONE (api-shape pass).** 41 sites renamed to `to_entries()`; the declaration's doc now leads with the cost (up to `JOURNAL_CAP` `LogLine`s, each its own `String`) and names the `generation` counter and the GUI cache as the evidence the old name misled. The `push` body's "Poison-tolerant like `entries`" comment moved with it.
- [x] `mem-008` — needs `src/ui/journal.rs:175` and `src/ui/mod.rs:169` — `LogLine { text: Arc<str> }` is the right fix (it makes the snapshot 500 refcount bumps instead of 500 `String` allocations, and `push` keeps paying the one real allocation it already pays), and it is **not applied**: the field is `pub`, so the type change is cross-cutting. Measured, exactly two sites break, both the `ui` agent's: `src/ui/journal.rs:175`'s `text: "refresh advised".to_owned()` becomes `.into()`, and `src/ui/mod.rs:169`'s `line.text.as_str()` becomes `&line.text`. Every other one of the ~45 `.text` uses is `.contains(..)` or a `{}` format argument, which `Arc<str>` serves unchanged through `Deref`/`Display` — checked one by one across `src/actuator/mod.rs`, `src/app/session/tests.rs` and `src/ui/mod.rs:486`. The report's fallback (`clone_entries_into(&mut Vec<LogLine>)`) was deliberately not added: its only caller would be `src/ui/mod.rs:144`, so it would land as an untested `pub fn` with no user, and it relocates the work rather than removing it. **DONE (final pass), and the measurement was exactly right.** `LogLine::text` is an `Arc<str>`. Precisely the two predicted sites broke — `ui/journal.rs`'s `.to_owned()` → `.into()` and `ui/mod.rs`'s `.text.as_str()` → `&*line.text` — plus two `assert_eq!`s inside `journal.rs`'s own tests that compare against a `&str`. Every other `.text` use is a `.contains(..)` or a format argument and was untouched, as measured. The `clone_entries_into` fallback stays **not** added, for the reason given.
- [x] `obs-003` / `obs-006` (journal half is **done**; the call sites remain) — needs `src/actuator/mod.rs:386-388,400-403`, `src/app/session/mod.rs:119-121,139,171`, `src/ui/mod.rs:230-236,256-261` — `EventLog::emit_at(level, lines)` now exists and `emit` is `emit_at(Level::INFO, …)`, so the severity-bearing callers become one-word edits (`journal.emit_at(tracing::Level::ERROR, &[…])`) instead of a hand-added second `tracing` macro beside each journal line — which is the shape that produced `obs-006` in the first place. `push`'s doc comment now states outright that it reaches the window and nothing else ("not the cheap `emit`, the forgetful one"), so the two `src/ui/mod.rs` sites that meant `emit` are now wrong on their face; they want `emit_at(Level::WARN, …)` (config not saved) and `emit_at(Level::INFO, …)` or `debug` (command dropped). `push` could not be demoted below `pub` to make the mistake unreachable: `examples/ui_preview.rs:118` consumes the crate externally. **RESOLVED (final pass).** Every severity-bearing caller now goes through `emit_at`: the actuator's halt and abort lines at `error`/`warn`, and the session loop's link-down and abort lines. The two `src/ui/mod.rs` sites this line called "wrong on their face" were re-read and are **right**, for a reason `push`'s doc could not carry: each already has its file half beside it, and a stronger one than `emit_at` gives — a `tracing::warn!` with `error`/`path`/`sections` as structured fields for the config write, and a `tracing::debug!` for the dropped command, at a level `emit_at` cannot express. Both now carry a comment saying so. `push` also stays `pub`: `examples/ui_preview.rs` still consumes the crate externally.
- [ ] `obs-012` — needs every `emit` call site — carrying an event `kind` field beside the prose means changing `emit`'s signature, which touches ~30 callers in `src/actuator/mod.rs`, `src/app/**` and `src/domain/**`. Filed P3 and explicitly optional in `22-obs.md`; not attempted. If it is ever done, `emit_at` is the seam to widen. **DECLINED (final pass), for a stronger reason than "P3, optional".** A `kind` is a property of each *line*, not of the `emit` call: of the 18 non-test call sites, six pass a `&lines` batch that `render.rs` assembled from a `Vec<Action>`, and such a batch is heterogeneous — a `MATCH` line and a buy line arrive together. Widening `emit`'s signature would therefore attach one kind to a mixed batch, i.e. record something *false* in a queryable field, which is worse than today's opaque-but-true `line`. Doing it right means the render layer producing typed `(kind, text)` pairs instead of prose, which is a change to the crate's presentation seam and wants its own review — not a rider on an observability sweep. `emit_at` remains the seam to widen if it is ever taken.

## From the `src/app/` agent (`mod.rs`, `session/mod.rs`, `session/tests.rs`)

- [x] `err-004` / `conv-004` — needs `src/error.rs` — **the prerequisite is complete**: `spawn_capture_with_budget` no longer uses the blanket `?`, it now says `.map_err(|source| Error::Capture(format!("starting the capture thread: {source}")))`. That was `Error::Io`'s only reachable producer, so **`Error::Io(#[from] std::io::Error)` (`src/error.rs:97`) now has zero producers in the crate** (grepped: every other `std::io::Error` in `src/` is built for `SurfaceError`/refusal text and never `?`-converted into `crate::Error`). The variant and its `#[from]` can be deleted outright, which is what `err-004` called the honest outcome. **DONE (api-shape pass).** `Error::Io` deleted outright — the honest outcome this line called for. See the `conv-004` note in the startup agent's section.
- [x] `api-001` — needs `examples/ui_preview.rs:159` — **six of the seven drop sites are done**: `session/tests.rs` now reads `let _ = ctrl.handle(…)` / `let _ = controller…handle(…)` at all six (`:581`, `:698`, `:746`, `:860`, `:1057`, `:1161`). The example is the last one; once it is `let _ =` too, `src/domain/` can add `#[must_use = "these actions are the decision — dropping them loses the refresh or the buy"]` to `Controller::handle` without reddening `-D warnings`. **DONE.** `examples/ui_preview.rs` already had `let _ =` at all five of its `handle` calls when triaged; the attribute is on. The real remaining count was 111 drop sites, in `domain/control/tests.rs` (87) and the `render.rs`/`ui/*` test modules (24) — invisible to both the domain and the app agent. All fixed.
- [x] `err-008` — needs `src/error.rs` — unchanged and deliberately not half-done. `app::supervise` still prints `format!("session error: {err}")`. Printing the chain there while `ConfigRead`/`ConfigWrite`'s `Display` still interpolates `{source}` would double-print the cause, so direction (a) needs the `Display` edit and the `supervise` edit in one commit. `supervise`'s call site is otherwise ready. **DONE.** `app::supervise` now says `format!("session error: {}", err.report())`; the `Display` edit and all five report sites landed in one pass. See the startup agent's `err-008` note for the correction to the report's `{err:#}` suggestion.
- [ ] `type-008` — needs `src/main.rs:272-291`, `src/ui/mod.rs:35,147-150` — `supervise` still returns `(String, bool)`. The `async-004` half **is** done (see below), so the only remaining change on this side is the return type itself, which breaks both callers the moment it lands. `SessionOutcome` should be declared where `main.rs` and `ui/mod.rs` can both name it.
- [x] `async-003` — needs `src/uplink/{mod,websocket}.rs` — still open, and the spawn site is ready: `crate::uplink::run(…)` at `src/app/mod.rs` can take `shutdown_rx.clone()` (the receiver is already subscribed and cloned for capture) the instant `run`/`run_with_connector` grows the parameter. Nothing was changed here to avoid a signature that only one side honours. **Note (api-shape pass):** if this lands in the same pass as `obs-001`, `run`/`run_with_connector` grow two parameters at once (`shutdown` and the URL type) — worth doing together so the three test call sites are touched once. **DONE (final pass), and the note about doing it with `obs-001` was right.** Both parameters landed in one change, so the three `run_with_connector` test call sites were touched once. See the uplink agent's `async-003` line for the three windows raced and the `wait_for_shutdown` shape.
- [x] `api-007` — needs `src/stream.rs:112-124` — the three `PipelineBudget::with_test_limits(a, b, c, d)` call sites in `app/mod.rs`'s tests are unchanged. They flip to `BudgetLimits { … }` in the same commit as the signature. **DONE.** All three `app/mod.rs` sites plus three more in `uplink/websocket.rs` now pass `BudgetLimits { … }`.
- [x] `obs-014` (`print_controls` half) — needs `src/render.rs:197` — gating the call at `src/app/mod.rs` behind `#[cfg(not(feature = "gui"))]` was written, measured and **reverted**: `print_controls` is `pub(crate)` with that one caller, so the gate makes it `dead_code` in every `gui` build, i.e. red under `-D warnings`. The definition needs the same `#[cfg]`, and that file is not in this set. The other two thirds of `obs-014` are done: the unknown-command line now goes through `journal.emit` (`stdin_loop`/`input_loop` take an `EventLog`), and `render_shop` was left alone as the report asked. **RESOLVED (final pass) — already fixed, by a better shape than this line records.** The `#[cfg(not(feature = "gui"))]` sits on `print_controls`' **body**, not on the item and not on the call, so the windowed build compiles a no-op and the one caller stays feature-independent — which is exactly why the reverted attempt (gating the call) produced `dead_code`. Its doc comment now says that, and names the real caller (`Session::run`, not `app::supervise` as it previously claimed).
- [x] `obs-002` (journal half) — needs `src/lib.rs` and `src/main.rs` (+ `examples/ui_preview.rs`) — still open. `app::setup(config)` would have to take `LogSetup::destination()` (or an `Option<PathBuf>`) to journal "there is no log file", and its three callers are all outside this set. **DONE (final pass).** See the startup agent's `obs-002` line: it landed in `main.rs`'s GUI `run_mode`, not by widening `app::setup`, and the reason is recorded at both arms.
- [ ] `mem-009` (the two `ArrayVec` halves) — needs `Cargo.toml` — `render_match`'s `Vec<String>` + `join(", ")` is gone (the slot numbers are written straight into the line), which is the dependency-free part. `submit_buys`' `rows: Vec<u8>` and `plan.rs:522`'s still allocate; `ArrayVec<u8, 6>` needs the `arrayvec` dependency, and the report itself marks that part optional. **DECLINED (final pass).** The dependency-free part was already done (`render_match`). For the rest: `submit_buys`' `rows` is a `Vec<plan::Row>` — a **1-byte** element type, built once per shop message a couple of seconds apart — so the allocation being removed is about six bytes, and `buy_job`'s `rows.to_vec()` is already exact-size. The report marks this part optional and `mem-smallvec` asks for a profile first; there is no profile, and no plausible one in which this appears. Recorded as a no, not as a not-yet.
- [x] `api-002` / `obs-001` — **correction for whoever finishes the field flip**: `app::redacted_server_url` is **not** dead. It is still the redactor for the `relay started` line (`info!(server = %redacted_server_url(&config.server_url), …)`), which is the crate's only remaining URL log. Deleting it must land together with `config.server_url.redacted()`, not before. **RESOLVED, and the correction was right and load-bearing:** `redacted_server_url` was indeed still the redactor for the `relay started` line, so the deletion landed together with `config.server_url.redacted()` at that exact site, not before.
- [x] `trait-` pointer (`CaptureStop::stop` is infallible in all three implementors) — needs `src/capture/mod.rs:47` — the permanently dead `if let Err(err) = self.stop.stop()` branch at `CaptureWorker::stop_and_join` is unchanged. Recorded because `11-trait.md`'s "one pointer outside this category" asked that the synthesis not lose it; narrowing the trait's return type is the capture module's call, not this file's. **DONE (final pass).** `CaptureStop::stop(&mut self)` returns `()`. All three implementors returned `Ok(())` unconditionally, so `CaptureWorker::stop_and_join`'s `if let Err(err) = …` was an unreachable branch logging an error that could not happen — and a `Result` a trait cannot produce is worse than no `Result`, because the handling it demands is untested by construction. The trait doc now says so, and says what a future fallible implementor must do instead (widen it back, with the caller's recovery written at the same time).

## From the newtype pass (`type-001` / `type-002` / `type-003`, cross-cutting write access)

- [ ] `type-003` (the domain half) — needs `src/domain/shop.rs:193` (`effective_slot`), `src/domain/control/mod.rs:153` (`BuyTarget::slot`), `:559`, `src/domain/control/watchdog.rs:152`, `src/ui/view.rs:47,96`, `src/ui/shop.rs:77` — **the money-losing half is closed**: `plan::Slot` (1-based) and `plan::Row` (0-based, `0..=MAX_ROW` *by construction*) exist, `row_for_slot`/`buy_zone`/`buy_job` take them, and `plan::buy_job(trigger, timings, epoch, &slots, now_ms)` is now `expected &[Row], found &[Slot]`. The conversion lives in exactly two `const fn`s — `Slot::row` and `Row::slot` — and `submit_buys` (`src/app/session/mod.rs:661`) is the single crossing, calling `plan::Slot::new(target.slot).row()` on the way in and `row.slot().get()` for the journal line. What the report additionally wanted — `effective_slot -> Slot`, `BuyTarget::slot: Slot`, `SlotRow::slot: Slot` — was **deliberately not done**: `Slot` would then need a home both `src/domain/` and `src/actuator/` can name, and today those two trees have *zero* dependency on each other in either direction (checked). Putting `Slot` in `domain/shop.rs` makes `actuator/plan.rs` — documented as the pure "no I/O, computed from a client rect, an epoch and a seed" module — depend on the wire-decoding domain; putting it in `plan.rs` makes the domain depend on the input backend. Either edge is an architecture decision that wants its own review, not a rider on a type-safety fix. Whoever takes it: `plan::Slot` is already the right shape (`new`/`get`/`row`, `Copy`, `Ord`), so the move is a re-home plus deleting the `Slot::new` at the one crossing.
- [x] `type-005` — needs `src/domain/shop.rs:122,193` and `src/uplink/protocol.rs:29-31` — **not taken, and it does not fall out of the three newtypes.** `Slot`'s `new` accepts any `u8` on purpose (a degraded shop reports `0`, and `effective_slot` clamps to `u8::MAX`), so the `0`-sentinel is still interpreted by `effective_slot` rather than being unrepresentable. The report's own fix pairs it with `type-004`'s `CatalogId` and a `deserialize_with` on `ShopItem`/`PurchaseNotice`; none of that is reachable from `Epoch`/`Slot`/`Row`/`Hwnd`. The `id` half's re-derived-sentinel bug is already fixed (see the `src/domain/` agent's line above). **DONE (final pass) for the `id` half; the `slot` half stays open, deliberately.** `CatalogId` landed — see the domain agent's `type-005` line for the shape. The `slot` sentinel is *not* closed: `Slot::new` still accepts any `u8` and `effective_slot` still clamps, because narrowing it is `type-003`'s domain half, which needs a home both `src/domain/` and `src/actuator/` can name and is excluded from this run as an architecture decision.
- [x] `type-010` (`game_port: NonZeroU16`) — needs `src/config.rs`, `src/app/mod.rs:1108`, `src/main.rs:204`, `src/capture/pcap.rs`, `src/capture/ip.rs` — **not taken.** It shares only a rule name with the three P1s: nothing in the epoch/slot/row/handle work touches the config type or the BPF filter string, so bundling it would have widened this diff across two more subsystems for no shared code. The existing item under the config agent's section is still the accurate description of what remains. **DONE (final pass).** See the config agent's `type-010` line for what landed and for why the `kinds` half took a different shape.
- [ ] `type-004` (the currency half) — needs `src/domain/control/mod.rs:183-188,251`, `src/domain/shop.rs` (`RefreshMeta`, `ShopItem::price`, `PurchaseLimit`), `src/uplink/protocol.rs` (`PurchaseNotice::gold`), `src/ui/view.rs`, `src/ui/statusbar.rs:81-82` — **filed by the final pass, which closed the other half.** `CatalogId` landed (see `type-005`), so the *identifier* space is no longer assignable from any counter. `Gold(u32)` and `Crystals(u32)` did **not**, and the hazard the report named is entirely theirs: `plan_targets` compares `item.price` against `self.gold_balance` while `stop_reason` compares `progress.spent` against `limits.max_spend` in *crystals*, and the two ledgers are one type apart — comparing a crystal budget to a gold price would compile and would silently veto or authorise every buy. Left open because it is a second, independent sweep (two types, ~8 fields, both test suites, plus the `Display`-through-`render::grouped` half that fixes `stat_tile`'s label↔value pairing) with no overlap with the id work beyond a shared report; bundling it would have made one unreviewable diff out of two reviewable ones.
## From the `src/ui/**` + `src/render.rs` correction pass (`own-002` re-fix)

- [ ] `own-002` (step 1, the `Arc` half) — needs `src/domain/control/mod.rs:274` (`last_snapshot: Option<ShopSnapshot>`) and `:343` (`last_snapshot()`) — **step 2 is done and closes the defect**: `ui::view::SlotRows` now re-derives the slot table only when the shop or the checklist behind it moved, so a repaint no longer clones a name per slot under the controller lock. What remains is not a fix, it is a simplification of the one the window had to use. Because `last_snapshot` is stored *inline*, a replacement lands at the same address and `Arc::ptr_eq` is unavailable, so the cache's staleness gate is `SlotRow::matches` — a field-by-field comparison, allocation-free but O(slots) and, more to the point, a second copy of the projection that has to be kept in step with it (`matches` destructures `Self` so the compiler catches a field added to one and not the other). Holding the snapshot behind an `Arc` collapses the whole gate to one pointer test plus the checklist compare, and `SlotRow::matches` can then be deleted. Whoever takes it: the checklist half of the key must stay either way — `wanted` is not in the snapshot, so a confirmed buy changes the rows while the shop on screen is byte-for-byte identical (`slot_rows_are_re_derived_when_the_checklist_moves_under_them` is the test for exactly that, and it must keep passing).

## From the `src/app/` remediation pass (`mod.rs`, `session/{mod,tests}.rs`) — second visit

- [ ] `trait-001` (P2, **previously unrecorded — this line is the record**) — needs `src/actuator/mod.rs:150,270` and `src/actuator/win.rs:565` — the finding was excluded from both the actuator agent and the app agent and then never reassigned, so it survived with no ledger entry at all. `match config.actuator.backend` is still at `src/app/mod.rs:467-493`: the backend is read from `config.toml` at **runtime** yet dispatched **statically**, so two six-argument arms that differ only in their first argument monomorphise `run_executor` — a large hand-rolled `async` state machine — twice into a single-file exe, for a vtable saving that is unmeasurable next to the 30–170 ms `std::thread::sleep` every `Surface` method already pays. The `app/mod.rs` side does **not** stand alone: collapsing the arms requires `run_executor(mut surface: impl Surface, …)` to become `Box<dyn Surface>` (or `&mut dyn Surface`), which is `src/actuator/mod.rs`. Nothing was changed here, deliberately — `job_rx` is *moved* into whichever arm runs, so there is no partial factoring that compiles without the erased parameter. Worth keeping from the report even if the dispatch change is declined: the `Surface: Send` supertrait, which turns the prose-only requirement at `src/actuator/win.rs:565-567` ("the handle is stored as an integer so the executor's future stays `Send` across awaits") into a compiler check that fires at a future backend's own `impl` instead of at a `tokio::spawn` two modules away. `SurfaceJobGuard`'s `S: Surface` bound stays either way (its `Drop` needs it — see `_FIX_BRIEF.md`'s do-not-touch list); the simpler route is `SurfaceJobGuard<'a>(Option<&'a mut dyn Surface>)`, which also drops a type parameter.
- [ ] `lint-unreachable` (new, needs `Cargo.toml`) — **`clippy::unreachable` now measures 0 across `--lib --bins` on all six lanes** and can be added to `[lints.clippy]` at `deny`. It was in neither the crate-root `cfg_attr` (`src/lib.rs:26`, which covers only `clippy::unwrap_used` and `clippy::panic`) nor `[lints.clippy]` — the only `unreachable` token in `Cargo.toml` is `unreachable_pub`, an unrelated rustc lint — so `unreachable!` was the one panic macro that could reach shipped code untripped. Both production sites are gone: `Resync::from_u8` (`src/app/mod.rs:122`) now returns the conservative `Self::Pending` plus a `#[cold] #[inline(never)]` `report_unknown_resync` (`error!` + `debug_assert!(false, …)`), matching `stream.rs`'s `report_pending_underflow` policy verbatim; and `capture_loop_budgeted`'s `Full(_)` arm (`src/app/mod.rs:1208`) no longer exists — the arm matches `Full(event)` and reads the drop charge off the `capacity` local instead of re-destructuring `CaptureEvent::Budgeted`, so the impossible second arm is structurally absent rather than asserted away. Measured, probe-validated (injecting one `unreachable!` scores 1, so the count is real and not a stale-cache 0): **0 hits on all six lanes for `--lib --bins`, and 0 on `--all-targets` as well** — there is not one `unreachable!` token left anywhere under `src/`, `examples/`, `tests/` or `build.rs`, so the entry needs no `--lib --bins` gate and no per-site `#[expect]`.
- [ ] `type-008` — **re-confirmed as correctly recorded and still declined here**; see the two existing lines (startup/observability agent, and the `src/app/` agent's own section). `supervise` is `-> (String, bool)` at `src/app/mod.rs:742`. Changing it is not doable inside `src/app/` alone: `src/main.rs:227` destructures the tuple into a `SessionErrorSlot` plus an `Arc<AtomicBool>` that `src/ui/mod.rs:35,147-150` also reads, so the `SessionOutcome` type has to be declared somewhere both can name it and both callers break in the same commit. No partial change was made — a `SessionOutcome` that only `supervise` returns and that `main.rs` immediately splits back into a string and a bool would be the same defect with more code.

## From the `src/actuator/` agent (`mod.rs`, `plan.rs`, `shield.rs`, `win.rs`)

- [x] `trait-002` — **DECLINED, recorded here so the next audit does not re-derive the measurement.** `WinSurface<D: InputDriver = SystemInputDriver>` does not compile as a shipped shape: `InputDriver` and `SystemInputDriver` are both private to `mod win`, so a type parameter bounded by them makes the `pub struct WinSurface` leak a private type (`private_bounds` / `private_interfaces`, both warn-by-default and therefore red under CI's `-D warnings`), and `run_executor(WinSurface::default(), …)` at `src/app/mod.rs` becomes a hard error. Widening `InputDriver` to `pub` to fix that would publish the seven-method Win32 shim the fake exists to intercept — a worse trade than one `Box` per session. The argument was already written at the declaration (`src/actuator/win.rs`, on `WinSurface`); this line is the ledger half so it is findable from the audit rather than only from the code. `Box<dyn InputDriver>` stays.
- [x] `api-004` — **DONE, the real typestate fix, not the string collapse the earlier commit claimed for it.** `Surface` has an associated `Window`; `acquire` returns `(Self::Window, ClientRect)` and `click`/`scroll`/`release` take `&Self::Window`. Deleted with it: both `Option<Target>` fields, both `target()` methods, the `NO_TARGET` const, and `SurfaceJobGuard`'s `.expect("active surface job guard")` — the guard now owns `&mut S` and `S::Window` outright with a `bool` for release idempotence, which is all the `Option` was ever tracking. `WinSurface` has no `release` at all any more (clearing the target *was* its body) and `MessageSurface` is fieldless, keeping only the `Drop` that lowers the shield. Two notes for a later pass. (1) `win::Target` had to become `pub` — an associated type on a `pub` trait impl for a `pub` type cannot name a module-private one (`private_interfaces`); it stays opaque (every field and method private) and its doc says why the `pub` is there. `MessageSurface` is `struct MessageSurface {}` rather than a unit struct for the same kind of reason: `src/app/mod.rs` spawns it with `::default()`, which on a unit struct is `clippy::default_constructed_unit_structs` in a file this type does not own. (2) **Two tests were deleted, not weakened:** `win.rs`'s `release_clears_target_and_actions_fail_closed` and `message_surface_input_without_acquire_is_fatal_not_a_panic` both asserted that an input with no live acquire answers `Fatal` rather than panicking, and that call can no longer be written — there is no `Target` to present. A comment sits at each site saying so. What they were really guarding is now pinned positively by a new `actuator/mod.rs` test, `every_input_and_the_release_see_the_window_that_job_acquired`, which gives the fake surface a per-acquire window token and asserts every `click`/`scroll`/`release` across two back-to-back jobs (the first aborted mid-way) saw *that* job's window. Net lib test count: 589 → 588.
- [x] `const-003` regression (`plan.rs`'s `the_row_count_and_the_scroll_split_stay_one_fact`) — **the guard test the `const-003` fix shipped could not fail.** Every one of its six assertions was derived from `MAX_ROW`/`LAST_TOP_ROW`, so each was a tautology: `Slot::new(MAX_ROW + 1).row() == Row::new(MAX_ROW)` holds for any `MAX_ROW < 255` by `Slot::row`'s definition, two clauses reduced to `MAX_ROW + 1 > MAX_ROW`, and the two `buy_job` clauses restated `row.get() > LAST_TOP_ROW`. `LAST_TOP_ROW = 2`, `= 4` and `MAX_ROW = 7` each passed the whole suite while `buy_job` planned a scroll-to-bottom for a row still at scroll-top — i.e. a click on the wrong item's Buy button, which is the exact hazard `const-003` was filed against. Now literal (`MAX_ROW == 5`, `LAST_TOP_ROW == 3`, `Slot::new(6)`/`(7)`, `Row::new(6)`) plus the clause that actually couples the count to the geometry: every row in `0..=LAST_TOP_ROW` has `buy_zone(row, false).cy` inside `0.0..=DESIGN_H`, and every row above it has `buy_zone(row, true).cy` inside while `buy_zone(row, false).cy` is outside. Verified by mutation: with the literal pins temporarily disabled, the geometry clause alone turns red for `LAST_TOP_ROW = 2`, `LAST_TOP_ROW = 4` **and** `MAX_ROW = 7`, and green again on restore.
- [x] `win.rs`'s misattached doc block — `wheel_wparam` was inserted between `pack_point`'s doc comment and `pack_point`, so the unbroken `///` run documented `wheel_wparam` with `MAKELPARAM` coordinate packing plus two paragraphs about another function's failure mode, and rendered `pack_point` — the click-coordinate packer — blank. Not a rustdoc diagnostic, which is why the gate stayed green. The block is back above `fn pack_point`, and its "the old form worked by accident" is present tense now that it describes the shipped code.
- [x] `ensure_dpi_awareness`'s last-error read — `std::io::Error::last_os_error()` was read *after* `GetThreadDpiAwarenessContext` and `GetAwarenessFromDpiAwarenessContext`, breaking the rule this same file states at three other call sites. Neither getter is documented to set the slot, which is the point: "not documented to write it" is not "documented not to". Captured now as `let set_error = (set == 0).then(std::io::Error::last_os_error);` immediately after the setter, and the `if set == 0` branch became `if let Some(error) = set_error` — the same branch, with the value that belonged to that call. On the shipped GUI build winit has already set the awareness, so this fires on every launch.
- [x] `mem-004` (part 2), `doc-007` (`Surface::acquire`), `api-006`, `num-007`, `doc-010` — the silent gaps, all closed. `find_game_window` reads a `static GAME_WINDOW_TITLE_W: [u16; GAME_WINDOW_TITLE.len() + 1]` encoded in a `const` block with a compile-time ASCII `assert!`, so the ~60 `wide()` allocations a two-slot buy job used to make are zero; `wide` survives for the shield's class name (once per process) and its doc no longer claims the hot caller. `Surface::acquire` has an `# Errors` section naming both verdicts and stating that a failed acquire leaves no `release` owing. The three job builders carry `#[must_use = "a planned job that is never submitted is a lost click"]` (all 30-odd call sites bind or forward the value — grepped, nothing to fix elsewhere). `to_screen`'s two `round() as i32` carry the comment naming the guard that makes them `NaN`-free. `to_screen`, `Slot::row` and `buy_zone` have `# Examples` lifted from their unit tests, taking the crate from 1 doctest to 4; `buy_zone`'s uses a `#`-hidden `row()` fixture so the `expect` stays out of the rendered example.
- [ ] `proj-009` (second half) — needs `Cargo.toml` — `src/config.rs` → `src/config/mod.rs` is done, so the crate is now 11 `mod.rs` files and zero exceptions; the `[lints]` table that pins it (`clippy::mod_module_files = "warn"`, plus `proj-010`'s `unreachable_pub = "warn"` under `[lints.rust]`) still has to be added. Verified `RUSTFLAGS="-W unreachable_pub" cargo check --locked --all-targets --no-default-features` is at **zero** hits, so the rust half can be turned on without fixing anything first.
- [ ] orphaned doc comment on `config::Config` — **not** mine to touch under the pure-move brief, but it is a live rustdoc defect: `src/config/mod.rs:48-49` is `/// Ceiling on a single `[actuator.timings]` extra wait, in milliseconds.` followed by a bare `///`, left behind when `MAX_TIMING_MS` moved down to `plan` (the const's body went, its doc block did not). It is now attached to `#[derive(Debug, ...)] pub struct Config`, so `cargo doc` renders that sentence as `Config`'s one-line summary and `Config` has no doc of its own. No rustdoc diagnostic fires on it, which is why the 0-warning gate never caught it. Delete the two lines, or give `Config` a real summary.

## From the `src/app/**` agent (module-decomposition pass, `proj-001`)

- [x] `proj-001` — **DONE, pure move.** `src/app/mod.rs` 2550 → six files: `mod.rs` (621/508 prod), `pressure.rs` (237/194), `ingest.rs` (370/228), `reassembly.rs` (762/282), `workers.rs` (590/219), `console.rs` (96/72), plus `#[cfg(test)] fixtures.rs` (45). The channel-topology safety argument was **verified, not assumed**: no two of the five modules share mutable state: `ingest`↔`reassembly` share only `pressure`'s `mpsc<CaptureEvent>` and `Arc<AtomicU8>` marker; `workers` holds handles and never inspects a payload (its one direct cross-seam call is `spawn_capture_with_budget` → `ingest::capture_loop_budgeted` on the thread it creates); `console` touches only `Sender<Command>` + `watch::Receiver<bool>`. `07-conc.md`'s `controller` → `timings` nesting was **checked and not moved** — there is no `.lock()` in production code anywhere in the new files; every lock in `src/app/` outside test fixtures is still inside `src/app/session/`, which is untouched. The `Resync`/`PressureResync` `Relaxed` block and its "do not strengthen these back" rationale travelled verbatim into `pressure.rs`; the `const _: () = assert!(size_of::<CaptureEvent>() == 120)` now sits two items below `CAPTURE_EVENT_QUEUE` and directly under the enum it pins, still ungated apart from its original `target_pointer_width`. The five queue-depth constants each stayed next to what they bound: `CAPTURE_EVENT_QUEUE` went to `pressure.rs` with the channel it sizes, the other four stayed in the root where `setup`/`Session::run` create theirs. Visibility widened only where the move forced it, all `pub(super)`, never `pub(crate)`: `pressure::{CAPTURE_EVENT_QUEUE, CaptureEvent, PressureResync}` + `request`/`try_enqueue`/`is_blocking_segments`/`acknowledge` (`Resync`, `report_unknown_resync` and `PressureResync::state` stayed private), `ingest::{capture_loop_budgeted, capture_loop}`, `reassembly::reassemble_loop_with_pressure`, `workers::{CaptureWorker, SessionWorkers, new, spawn, shutdown, spawn_capture_with_budget}`, `console::stdin_loop`, and the three `fixtures` builders. Nothing else moved out of file-private, because a parent's private items are already visible to its children — `ShutdownSignal::new`/`subscribe`, `actuator_mode` and `build_source` needed no widening at all. **One deviation from the report's file list, deliberate:** `ParseCommandError` and `impl FromStr for Command` stayed in the root rather than moving to `console.rs`. `proj-001` listed a free `parse_command` in `console.rs`, but that function is now a `FromStr` impl on `Command`, which the report's own table keeps in the root — and the impl's doc links `[`Filter`]`/`[`Limits`]`/`[`plan::Timings`]`, which resolve in the root and would have needed either imports-for-docs-only or a rewritten rationale in `console.rs`. `console.rs` is 72 production lines, matching the report's ~55 estimate for the rest.
- [x] `proj-004` (the `src/app/mod.rs` half) — **DONE as part of `proj-001`, in the shape `proj-004` itself asked for.** Rather than one `src/app/tests.rs`, the 1170 test lines were distributed along the `proj-001` seam (`proj-004`: "then redistribute to `src/app/<submodule>/tests` as `proj-001` lands"), so no intermediate file was created: 1 test to `pressure.rs`, 3 to `ingest.rs`, 15 to `reassembly.rs`, 8 to `workers.rs`, 1 to `console.rs`, 9 (+1 `cfg`-duplicated arm) left in the root. The shared-fixture friction point it names is closed by `src/app/fixtures.rs` (`#[cfg(test)] mod fixtures`), holding exactly the three that straddled the seam — `initial_anchor_segment`, `initial_anchor_segment_in`, `segment_with_capacity`. The other eight it listed turned out **not** to be shared once the tests were sorted: `NoopStop`, `LiveGuard`, `blocking_capture`, `fake_actuator`, `BlockingSource`/`BlockingStop`, `ImmediateErrorSource` and `finished_capture_worker` are used only by `workers`' tests, `EnableOnFirstSegment`/`LosingSource` only by `ingest`'s, `recv_exact` only by `reassembly`'s — each now lives beside its one consumer, so no fixture visibility had to be widened for them and `CaptureWorker`'s fields stayed private. The three `#[cfg(test)]` shims survive as `proj-001` predicted: `capture_loop` is `pub(super)` in `ingest.rs` (its callers are `ingest`'s own tests, but it is also the shorter-signature entry the report describes), `reassemble_loop` and `spawn_capture` stayed *private* to their new files because their only callers moved in with them.
- [ ] stale comment path — needs `src/error.rs:106` — the comment says "`std::thread::Builder::spawn` in `app::spawn_capture_with_budget`"; that function is now `app::workers::spawn_capture_with_budget`. Prose only, no diagnostic, one-word fix. (`src/capture/mod.rs:8`'s `app::build_source` is still accurate — `build_source` stayed in the root, per `proj-001` note 2.)
- [ ] line-number drift in three open items above — needs nothing but a reader who knows: `trait-001`'s actuator-backend arms are still in `src/app/mod.rs` (`Session::run`, now ~`:300-330`, not `:467-493`); `type-008`'s `supervise` is still in `src/app/mod.rs` (now ~`:380`, not `:742`); `lint-unreachable`'s two cited sites moved — `Resync::from_u8` is `src/app/pressure.rs` and `capture_loop_budgeted`'s `Full(event)` arm is `src/app/ingest.rs`. Both of `lint-unreachable`'s premises still hold; the file names in its text are the only stale part.

## From the `src/ui/editor/**` split agent (`mod.rs`, `timing_meter.rs`, and the new `hunt.rs`/`stop.rs`/`timing.rs`)

- [ ] `proj-008` (the draft-grouping prerequisite) — needs nothing but a pass willing to change `EditorState`'s shape — **the mechanical half is landed and is a pure move**: `src/ui/editor/{hunt,stop,timing}.rs` (204 / 163 / 177 lines) took 456 lines out of `mod.rs` (1227 → 771) with **zero** `EditorState` fields widened, no signature changed, and the same 26 tests. The seam used is *shell state, not topic*: a section file holds what works on a `Filter` / `Limits` / `Timings` value handed in by the caller. What is **not** landed is the `HuntDraft`/`StopDraft`/`TimingDraft` grouping, and after trying it the report's premise turns out to be only half right. The field clustering **is** clean — the twelve fields partition perfectly and nothing outside `src/ui/editor/` reads one (`grep -rn "EditorState" src/ examples/`: `src/ui/mod.rs` names only `new`, `mark_applied`, `edit_sections`, `commit_row`) — but grouping **alone does not avoid the widening the report used to justify it**. With private draft fields, `EditorState::new`, `mark_applied`, `edit_sections` and `commit_row` all still need field access from `mod.rs`, and so do 15 of the 21 harness tests (`editor.filter = …`, `editor.timing_open = true`, `assert_ne!(editor.filter, editor.applied_filter)`), which cannot move into a section file because they drive the whole Setup surface through `edit_setup`. Closing that gap needs roughly sixteen new functions that do not exist today (three constructors, three `is_dirty`, three `&mut open` accessors, three per-draft command builders, three per-section `render` entry points) and it fragments three of the comments the audit leaned on — `commit_row`'s bit-exact `Option<f64>` rationale, `mark_applied`'s deliberately exhaustive `Command` dispatch, and `edit_sections`' collapsed-bar spacing rhythm (pinned by `collapsed_sections_tile_with_no_hover_gap`) — across four files. That is a redesign of the module's internal contract rather than a prerequisite to a move, so it was declined instead of forced mid-split. Acceptance criteria for whoever takes it: those three comments survive intact, `mark_applied` keeps its exhaustive match (no wildcard), and `arm_optional` + `optional_field` stay **one** copy each — they are the shared arming/value halves of all three "optional numeric with a checkbox" widgets and the `clamp_existing_to_range(false)` fix reached by `seeded_zero_limit_is_not_silently_clamped` lives there once. They are deliberately parked in `mod.rs`, one level above both consumers, for exactly that reason.
- [ ] `proj-008` (the four functions still in the shell) — needs the grouping above — `hunt_body`, `stop_body`, `timing_body` and `preset_row` are the only section code left in `mod.rs`, and only because each takes `&mut EditorState`: `hunt_body` reaches four fields (`filter` plus the three scratch buffers), `timing_body`/`preset_row` two (`timings`, `fine_tune_open`), `stop_body` one (`limits`). Narrowing their parameters instead of grouping was **not** taken — the split brief forbids signature changes, and it lands asymmetrically anyway (1, 2 and 4 parameters), which is the shape the report's own `HuntDraft` sketch exists to avoid. About 180 lines of follow-on move once either route is chosen; the three new files already carry a module-header note pointing here.

## From the `src/capture/pcap.rs` split agent (now `pcap/{mod,link,sys}.rs`)

- [ ] `proj-005` (stale path references to the deleted `src/capture/pcap.rs`) — needs `build.rs` and `src/crash.rs` — two files outside the grant name the old path and are now wrong: `build.rs:9` documents the backend as "(`src/capture/pcap.rs`)" (should be `src/capture/pcap/`), and `src/crash.rs:192`/`:198` use the string `"src/capture/pcap.rs:60"` as the fake panic location in `a_crash_entry_records_the_location_and_the_message` — a fixture, so the test still passes, but `21-doc.md`'s advice to use a real path no longer holds for it. One word and one string literal.
- [ ] `proj-005` (the `devices`/`worker` half of the proposed four-way split) — **DECLINED, with reasons, not deferred.** The report offered two shapes and its own second one was taken: `ffi` + `link` out, `capture_loop` left beside `Handle`. In this tree the `ffi`/`devices`/`worker` boundary cannot be drawn without touching the two things `04-unsafe.md` and `13-const.md` protect. Putting `Wpcap` in `ffi.rs` and its callers in `devices.rs`/`worker.rs` requires `pub(super)` on **11 of the 13 verified `wpcap.dll` function-pointer fields** (only `geterr` and `lib_version` are called from `Wpcap`'s own impl), and rustfmt then re-wraps at least `open_live` and `next_ex` — i.e. the ABI transcription two reviews checked against `pcap.h` gets reformatted as a side effect of a move. Splitting `open_device` from `capture_loop` additionally requires `pub(super)` on `Handle::handle`, which is the `*mut PcapT` that `unsafe impl Send for Handle` reasons about. So the pointer layer was kept whole as `pcap/sys.rs` (739 lines) and the invariant was **strengthened** instead: `handle` is private to that file, `grep -rn PcapT src/` matches nothing outside it, and the module header states the containment so a reader can check the `Send` argument against one file rather than three. If a future pass wants `sys.rs` smaller, the honest prerequisite is accessors on `Wpcap` (`fn next_ex(&self, …)` wrappers, one per call site) so the 13 fields can stay private — that is a real API addition, not a move, and it should be argued on its own terms.

## From the `src/actuator/win.rs` split agent (now `win/{mod,dpi,send_input,post_message}.rs`)

- [ ] `proj-004` (the `win.rs` half) — needs a pass willing to add a nesting level under `win/` — `proj-007` landed and reduced this item to **one** file. Tests now sit beside the code they test in each of the four: `mod.rs` 418 prod / 70 test, `dpi.rs` 136 / 41, `post_message.rs` 251 / 154, `send_input.rs` 337 / **624**. Only `send_input.rs` is still test-majority, and closing it means `win/send_input/{mod,tests}.rs` — the crate's own shape at `app/session/` and `domain/control/`, but applied to one of four siblings, so `win/` would read `{mod,dpi,post_message}.rs` + `send_input/`. Not taken unilaterally: it is a directory-shape decision belonging to `proj-004`, not a consequence of `proj-007`. Nothing blocks it — the 23 `FakeInputDriver` tests need no visibility change (`use super::*` plus the existing `crate::actuator::win::tests::{GAME_HWND, OTHER_HWND, game_rect, uipi_refusal}` import travel verbatim), and `win::tests`' `pub(super)` fixtures are already reachable from a grandchild.
- [ ] rustdoc cannot link into a module-private item — needs nothing but a reader who knows why four items are `pub(super)` — `post_message::{post, pack_point, post_refusal}` and `send_input::sendinput_result` are `pub(in crate::actuator::win)` **for their doc links only**, not for any call site: `Hwnd`'s rationale doc (the `post(lparam, …, target.hwnd)` transposition the newtype exists to stop) lives in `win/mod.rs`, and `ensure_dpi_awareness`'s doc cites `sendinput_result` as the reason the awareness must be *read back*. `cargo doc --document-private-items` resolves intra-doc links under normal visibility rules, so leaving them module-private meant deleting four links out of two of the longest rationale comments in the crate — refused, since `[lints.rustdoc]` denies `broken_intra_doc_links` and those comments *are* the findings. Each backend module header records this at the top. If a later pass wants them private again, the links have to become plain code spans and the two doc comments say one thing less.
