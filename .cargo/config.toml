# Static linking: WinDivert's user-mode code is compiled into the exe (from the
# C sources vendored by windivert-sys, via MSVC). No WinDivert.dll to ship — only
# the WinDivert64.sys driver remains, embedded in the exe and extracted at runtime.
[env]
WINDIVERT_STATIC = "1"
